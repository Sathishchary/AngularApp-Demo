import { HighletDirective } from './highlet.directive';

describe('HighletDirective', () => {
  it('should create an instance', () => {
    const directive = new HighletDirective();
    expect(directive).toBeTruthy();
  });
});
